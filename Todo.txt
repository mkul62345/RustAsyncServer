mw_auth has commented out blocks due to breakage

_dev_utils:
    dev_db.rs : fix the db string to be fetched from env 



Add variable field support for generic base "create" function and "upadate" function






Fresh empty db + devutils_OFF + cargo run  ===> Error: Model(Store(FailToCreatePool("error returned from database: password authentication failed for user \"pg\"")))

db off + cargo run ===> Runs 

db off + devutils_ON + cargo run ===> Called unwrap on PoolTimedOut

db on + devutils_ON + cargo run ===> Success

db populated + devutils_OFF + cargo run ===> Success